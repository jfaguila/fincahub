[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']

[phases.install]
cmds = ['cd api && npm install']

[phases.build]
cmds = ['cd api && npx prisma generate', 'cd api && npm run build']

[start]
cmd = 'cd api && npx prisma db push --skip-generate && node dist/src/main'
